# super-board pre-flight — Omega build pipeline

> Generated by `super-board lint`. Each `[ ]` is a **halt gate** for `super-board run` —
> the loop refuses to start (or a worker bounces the card to Blocked) until the items
> its ticket needs are `[✓]`. Scanned issues: **#1 Add coverage test in ctest**.
> Probe host: `mac141243` (Darwin arm64), 2026-06-17.

## ⚠️ Top-line verdict

**Issue #1 cannot be built or tested on this host.** The Omega standalone build requires a
**CIME-supported machine** (this macOS arm64 laptop is not in `cime_config/machines/config_machines.xml`),
and issue #1's **device-side GPU coverage is a v1 requirement (decision 1)** — that needs NVIDIA/AMD/Intel
GPUs and their toolchains (`nvcc`/`hipcc`/`icpx`, `ncu`/`rocprof`), **none of which exist here**.
Run this pipeline on a supported HPC machine (e.g. Perlmutter for CUDA, an MI-series box for HIP,
an Intel GPU node for SYCL), **or** narrow issue #1 to a host-only CPU subset before running here.

## 🔑 Credentials the loop will need

- [ ] `CODECOV_TOKEN` — Codecov upload (issue #1, decision 4). Unset in this env.
- [ ] CDash project + drop credentials — `ctest_submit` coverage submission (issue #1, decision 4). Omega already calls `ctest_submit` in `CTestScript.cmake`; confirm the CDash project/auth.

## 🛠 Tools the loop will need

Host build toolchain:
- [✓] `gh` CLI authenticated (`grnydawn`)
- [✓] `git`, `cmake`, `gcc`/`g++`, `clang`/`clang++`, `mpicc`/`mpicxx`, `make`, `gcov`
- [ ] `ninja` (optional generator) — missing

Host coverage tools (decision 2 — both toolchains):
- [ ] `lcov` + `genhtml` (gcov/lcov path) — missing → `brew install lcov`
- [ ] `gcovr` (optional lcov alternative) — missing
- [ ] `llvm-cov` + `llvm-profdata` (LLVM source-based path for Clang-derived targets) — missing → install full LLVM toolchain
- [ ] `clang-format` + `pre-commit` + `conda` (the `omega_dev` env from `components/omega/dev-conda.txt`) — missing → needed for the lint/format gate

GPU compilers + device-coverage tools (decision 1 — required for v1):
- [ ] CUDA: `nvcc` + `ncu` (Nsight Compute) — missing
- [ ] HIP: `hipcc` + `rocprof`/`rocprof-compute` — missing
- [ ] SYCL: `icpx` (intel/llvm build containing **PR #20710** for device coverage) — missing

## 🌐 Environment

- [ ] **CIME-supported machine** — REQUIRED to configure/build Omega standalone; the build pulls compiler/flags from CIME and host `mac141243` is not a recognized machine. An unsupported host cannot complete the `cmake -DOMEGA_CIME_MACHINE=...` configure step.
- [ ] **ParMETIS** (`OMEGA_PARMETIS_ROOT`) — required by the Omega build; not located on this host.
- [ ] **GPU hardware** (NVIDIA / AMD / Intel) — REQUIRED to exercise device-side coverage (decision 1); none on this host.
- [ ] **Test mesh files** — `OmegaMesh.nc`, `OmegaSphereMesh.nc`, `OmegaPlanarMesh.nc` linked into `components/omega/test/` (download URLs in `doc/devGuide/QuickStart.md`); needed by the ctest suite.
- [~] Required submodules (`externals/ekat`, `externals/scorpio`, `externals/YAKL`, `components/omega/external`, `cime`) — appear initialized in this checkout; re-verify on the target machine with `git submodule update --init --recursive`.

## Recommendation

`super-board run` is **attended-only** here and would correctly halt on issue #1 (no build possible).
Two paths:
1. **Move the pipeline to a supported HPC machine** with the GPU backend(s) you want covered, install the coverage tools above, then `super-board run`.
2. **Scope a host-only CPU milestone first** (Serial/OpenMP `--coverage` via gcc/lcov on a box that has `lcov`+`llvm-cov`), landing the CMake gate + final `COVERAGE_REPORT` test, then add the device backends on HPC. (This would re-open the device-in-scope decision for a v1.0 vs v1.1 split — your call.)
