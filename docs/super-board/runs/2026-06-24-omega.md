# super-board run — 2026-06-24 (Omega, project #7)

Backend: workflow · base branch `ykim/omega/coverage` · tier medium ·
orchestrator on Aurora login node `aurora-uan-0010`.

## Wave 1 — feasibility probe (no build, no PBS, no allocation)

Only selectable Ready card was #5 (SYCL device coverage). Per user direction the
orchestrator ran a cheap login-node feasibility probe instead of dispatching a full
SYCL/PBS wave, since #5 is gated on intel/llvm PR #20710. The probe was conclusive:
Aurora's release `icpx` 2025.3.2 ignores `-fcoverage-mapping` /
`-fprofile-instr-generate` for the `spir64-unknown-unknown` device target ("only
supported for host compilation") → no device-side coverage instrumentation. #5
moved to Blocked with full evidence.

| Card | Lanes run | Final status | Column | Detail |
|------|-----------|--------------|--------|--------|
| #5   | probe     | blocked      | Blocked | icpx 2025.3.2 lacks PR #20710 (device-coverage flags ignored for spir64); see `2026-06-24-issue5-sycl-coverage-probe.md` |

## Board after wave 1

- Done: #1 (coverage test in ctest)
- Blocked: #5 (SYCL device coverage — needs PR #20710 toolchain) ← human queue
- Backlog: #2 (CUDA+HIP device coverage — non-Aurora hardware)
- Ready / Building / QA / Review: empty

**Run drained.** No selectable cards remain (Backlog is not auto-pulled). Next
`super-board run` is a no-op until a card is dragged to Ready.
