QA Report — issue #1 "Add coverage test in ctest" — v1 (PASS)

Branch:   issue-1-add-coverage-test-in-ctest
PR:       #4
Machine:  Aurora — login node aurora-uan-0012 (configure) + PBS debug compute node x4216c0s4b0n0 (build + ctest)
Toolchain: icpx (oneAPI DPC++/C++ 2025.3.2), OMEGA_ARCH=SERIAL -> coverage toolchain=llvm
Result:   all in-scope ACs verified. 41/41 ctest pass; COVERAGE_REPORT is #41 (last) and PASSes.

HOW THE SUITE WAS RUN (reproduce / Reviewer re-run)
Configure on the Aurora login node (FetchContent needs internet), then batch build+ctest to a compute node:
  # submodules cime, omega externals, externals/ekat(+kokkos), externals/scorpio must be init'd in the worktree
  cmake -DOMEGA_CIME_MACHINE=aurora -DOMEGA_CIME_COMPILER=oneapi-ifx \
        -DOMEGA_ARCH=SERIAL -DOMEGA_BUILD_TYPE=Debug \
        -DOMEGA_COVERAGE=ON -DOMEGA_COVERAGE_THRESHOLD=50 \
        -DOMEGA_PARMETIS_ROOT=/lus/flare/projects/E3SM_Dec/youngsun/opt/metis \
        -DOMEGA_BUILD_TEST=ON -S <repo>/components/omega -B <build-dir>
  bash .claude/super-board/aurora/run-ctest-batch.sh <build-dir>
Threshold tuned to 50 (below ~56% baseline) so COVERAGE_REPORT PASSes end-to-end; at the shipped default 90 it FAILs by design.

ACCEPTANCE-CRITERIA VERIFICATION

Host-side:
  H1 COVERAGE_REPORT runs LAST, passes, prints total %  -> PASS  (41/41 Test #41 COVERAGE_REPORT Passed; prints 56.35%)
  H2 OMEGA_COVERAGE OFF by default, no flags when OFF    -> PASS  (empty CMAKE_CXX_FLAGS, OMEGA_COVERAGE:BOOL=OFF, no report, no helper)
  H3 OFF build keeps all existing tests unchanged       -> PASS  (same 40 unit tests registered)
  H4 SERIAL build reports NON-ZERO host line coverage   -> PASS  (TOTAL 56.35% line coverage)
  H5 Aggregate merged across ALL test exes              -> PASS  (226 .profraw merged into one coverage.profdata)
  H6 Toolchain split gcc->gcov / clang->llvm-cov        -> PASS  (icpx -> toolchain=llvm, -fprofile-instr-generate -fcoverage-mapping in compile+link rules)

Device-side:
  CUDA  -> N/A deferred to #2 (no NVIDIA on Aurora)
  HIP   -> N/A deferred to #2 (no AMD on Aurora)
  SYCL  -> PARTIAL/best-effort (no hard gate per decision 3): host LLVM path verified end-to-end; native device path
           (intel/llvm PR #20710) is experimental and the Aurora RELEASE icpx 2025.3.2 does not expose it;
           native_cpu fallback documented. See SYCL NOTE below.
  Per-backend method/maturity/limits in dev docs        -> PASS (Testing.md matrix matches issue matrix)

Threshold/reporting/gating:
  T1 Host gated default 90% (tunable), fails below; device best-effort no gate -> PASS (56.35% >= 50 PASS; gate logic 56<90 FAIL, 0 FAIL)
  T2 Report to CDash + Codecov, manual, no CI job       -> PASS (CTestConfig CTEST_COVERAGE_COMMAND gcov; CTestScript ctest_coverage(); codecov.yml; omega_coverage.sh; no workflow change)
  T3 Gated to Debug (error otherwise); tools in dev-conda.txt -> PASS (Release+cov FATAL_ERROR; gcovr+lcov added)
  T4 v1 device methods production/experimental only     -> PASS (no NVBit/HIP-PGO code)
  T5 Total excludes external/ + test/, src/ only        -> PASS (every reported file under components/omega/src/; 0 external/_deps/share/test files)

SYCL NOTE (honest framing)
The host SYCL llvm-cov path is fully verified (the SERIAL/icpx run uses the same LLVM toolchain and produces a valid
merged report). The native device-on-GPU SYCL coverage path depends on intel/llvm PR #20710, which is experimental and
present only in recent intel/llvm nightlies. The Aurora module env provides the oneAPI RELEASE compiler (icpx 2025.3.2),
whose --help does not expose the device-coverage feature, so the native device path cannot be exercised here. This is the
risk the issue body flags ("verify PR #20710 present") and the reason decision 3 makes device coverage best-effort with no
hard gate for v1. The implementation ships the documented native_cpu portable-fallback cross-check. Does not block the
host/CPU + SYCL-host v1 scope of this card.

KNOWN WARNING (non-blocking)
llvm-cov report prints "warning: 8579 functions have mismatched data" — the standard llvm-cov artifact when merging .profraw
across many executables that instantiate the same templated/inlined headers (Kokkos, Omega *.h) in different translation units.
llvm-cov still emits valid per-file and TOTAL numbers (line 56.35%, function 91.85%). Not an error; report + lcov export succeed.

ARTIFACTS
  coverage.info (lcov, ~2.7 MB) + coverage.profdata (merged, ~4.4 MB) — see coverage-artifacts.txt
  coverage-report-output.txt — full COVERAGE_REPORT output (per-file table + TOTAL + gate verdict)
  ctest-summary.txt — 100% tests passed, 0 failed out of 41; COVERAGE_REPORT #41 last
  configure-OFF.log / configure-ON-DEBUG.log / configure-RELEASE-COV.log — configure-time evidence
  ac-off-checks.txt / ac-on-checks.txt / gate-logic-test.txt / ctest-ordering.txt — AC-check transcripts
